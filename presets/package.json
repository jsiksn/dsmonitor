{
  "name": "dsmonitor-presets",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "description": "dsmonitor presets (stylingPolicy 4종 + config 템플릿) — 본 폴더는 dsmonitor 패키지 안 통합 (별도 발행 없음). 사용자 측 require(\"dsmonitor/presets/*\") 호환 위해 type:commonjs."
}
